import { styled } from '@linaria/react';

const DSFallback = styled.div`
  position: relative;
  overflow: hidden;
  width: 100%;
  height: 100%;
  background: var(--primary-gradient);
  border-radius: var(--border-radius);
`;

export { DSFallback };
